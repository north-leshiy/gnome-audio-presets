{
  "uuid": "devkit-helper@audio-presets.test",
  "name": "audio-presets devkit helper",
  "description": "Test-only: enables unsafe mode in the nested devkit Shell so tests can use Eval and Screenshot. Never install into a real session.",
  "shell-version": ["50"],
  "version": 1
}
